# M5.21.4: the 2-particle program: attraction/repulsion + the antipair capture-to-annihilation film

**Status**: ✅ CLOSED COMPLETE 2026-07-21 (go ~13:15 EDT, terminal review presented 15:45, approved same day; duration 2:30, no cap). Roadmap row: [`m5_roadmap.md § DONE`](../m5_roadmap.md) (appended at the end). (Was M5.21.2 before the 2026-07-17 run-order renumber; staged at the [M5.21](m5_21_task_details.md) close, the ANTIPAIR arm added 2026-07-16 at the scorecard sync.) The LAST physics row before the RENDERING UNLOCK marker: **the unlock TRIPPED at this close**: [M5.26](m5_26_task_details.md) → [M5.25](m5_25_task_details.md) are runnable on user "go".

## TASK PLANNING (2026-07-21)

**Scope**: the two-particle program on the verified-L 3D instrument (the [M5.21.2b](m5_21_2b_task_details.md) T2 term set + sym stencil). Two arms. (1) INTERACTION: same-charge (A + A) and opposite-charge (A + anti-A) two-defect states, the descent-grade interaction curve E(d) in both signs, compared against the first-principles Coulomb form implied by the [M5.16](m5_16_task_details.md) far-field anchor (the [M5.21.5](m5_21_5_task_details.md) discipline: derive the predicted coefficient first, no free factor). (2) THE ANTIPAIR: the full 3D dynamical capture-to-annihilation film with the charge + energy ledgers closed at dynamics grade, the remaining item of the electron-hunt antimatter row ([`m5_particle_hunt.md`](../m5_particle_hunt.md): the canonical-era ledgers were static; "remaining: the full 3D dynamical capture film").

**Why now**: the whole M5.21 series feeds this: the census electron A ([M5.21.2](m5_21_2_task_details.md)/2b), the certified damped-wave stack (leapfrog + sponge + closed ledger, [M5.21.6](m5_21_6_task_details.md)), the film adapter (true templates on 3D states), and the audit rigor bar ([M5.21.5](m5_21_5_task_details.md)). The roadmap note "prefers the best stable state from M5.21.2/M5.21.3" resolves to the seed-A electron family (the M5.21.3 verdict left no dynamical 4D state; the 3×3 statics + damped dynamics is the sanctioned arena, same as M5.21.6).

**The seed construction (the design decision of record)**: both defects sit on the z-axis, so the classic ANGLE-ADDITIVE meridional ansatz applies exactly: α(ρ, z) = Σᵢ qᵢ·atan2(ρ, z − zᵢ), n̂ = (sin α cos φ, sin α sin φ, cos α), M = R diag(1, δ, 0) Rᵀ with the [`m5_21_2b_a_instrument.py`](../scripts/m5_21_2b_a_instrument.py) `seed3` frame convention (v1 = n̂, v2 on the azimuth, v3 = n̂ × v2). This stays ON the (1, δ, 0) spectrum manifold everywhere (orthonormal frames, no blend seam, no admissibility repair), reduces to the census seed A at one defect, and makes the far fields literally additive: antipair far field → uniform v1 = +ẑ (vacuum-like up to the axial v2 through-line, which is part of A's own measured signature), same-charge far field → the degree-2 texture (pinned arena).

**Definition of done** (testable):

| # | Criterion |
| --- | --- |
| 1 | P0 gates green (cap 3 tries): pair/anti seed integrity (spectrum exact, far-field verdicts, single-defect regression to seed A); the NEW signed-charge instrument (ONE global continuity-oriented director lift + per-core Mermin-Ho B-flux, the [M5.21.5](m5_21_5_task_details.md) monopole convention: flux = charge) reads +1 (seed A), −1 (mirror-A), the antipair (+1, −1) with far sphere 0, and the same-charge candidate is ACCEPTED only if it measures (+1, +1) with far sphere +2 (constructions iterated within the cap; failure = the honest descope of the same-charge arm, never a relabel); flux stable across ≥ 2 sphere radii; dynamics-stack regression (leapfrog γ = 0 energy conservation + sponge dissipative on a pair state, the M5.21.6 bars) |
| 2 | The E(d) interaction curves, both signs; the attraction/repulsion verdict read REFERENCE-FREE from the sign of dE/dd across the ladder (no cross-state energy offset enters the sign claim); the Coulomb-form comparison uses E(d) − E(d_max) fit against the coefficient derived in the note § 1 BEFORE the measurement is read against it; equal-depth healed reads with core positions verified stationary |
| 3 | The antipair capture film at dynamics grade: d(t) core-separation trajectory, Q(t) per-core and total charge ledger, energy ledger E + KE + absorbed closing at the M5.21.6 bar, released-structure count through the merge; the ANNIHILATION-vs-DRAIN discriminator verdict (pre-registered below) |
| 4 | Films: BOTH true templates (basic + thermal) on the headline sequences via the meridional 4×4-embed adapter; seed + endpoint cross-section prints (standing rule); panel embedded in FINDINGS |
| 5 | Independent adversarial audit (own implementations) of every substantive claim before the review |
| 6 | Doc checker exit 0; heavy npz local + gitignored with the `data/_DATASETS.md` manifest regenerated; regen command + runtime per array documented here; checkpoints eager |

**Gating**: user "go" ✅ 2026-07-21 ("go 21.4, reset 2:50pm, ping me"). M5.21.1 ✅ closed; the series re-capture is folded below. Feeds: the RENDERING UNLOCK (roadmap marker trips at this close), the hunt antimatter row re-base, the next author outbound (BOTH the 21.5 and 21.4 notes ride it, user 2026-07-21).

**The M5.20/M5.21-series re-capture (series rule)**: the stack of record = T2 eigenvalue-penalty term + sym stencil (the 2b well-posedness fix; the 2h deep-descent checkerboard is a standing anti-recipe); leapfrog dt = 0.025 + absorbing sponge certified at M5.21.6 (ledger closes to 3 decimals, 23% radiated in the C → A decay); drain ≠ decay is a measured trap (the 21.6 B verdict) and gets a pre-registered discriminator here; equal-depth reads are labeled contained-not-converged (the 21.2/21.3 honesty label); films = both true templates via the meridional embed (the 21.2 review issue, fixed at 2b); the μ/g bridge discipline (derive the prediction before reading the measurement, M5.21.5).

**Phases**:

| Phase | Content | Verdict artifact |
| --- | --- | --- |
| P0 | Instruments + gates: `m5_21_4_a_pair.py` imports the 2b instrument (INS) + the 21.6 dynamics pieces; NEW code = the angle-additive pair seeds (q₁, q₂ at ±d/2), the signed-degree instrument (continuity-oriented v1 lift on lattice spheres + solid-angle sum; frustration reported honestly if the lift obstructs), the two-core tracker (v1-alignment minima / energy-density maxima); gates per DoD 1 | `m5_21_4_gates.json` |
| P1 | E(d) ladders at n = 32 (L = 48): d ∈ {12, 18, 24}, both signs; equal-depth healing descents (~1500 it, cores verified stationary); E_int(d) = E_pair(d) − 2·E_single (same box, same depth); sign verdict + the Coulomb-form fit against the § 1 derived coefficient; n = 48 spot-check at one d | `m5_21_4_ladder.json` + curve plot |
| P2 | THE CAPTURE FILM at n = 48 (L = 72), free BC: antipair at d₀ ~ 24, damped-wave evolution (interior γ small + sponge, the 21.6 recipe); reads = d(t), Q(t) per-core ±1 → total 0, the closed energy ledger, `loop_read` component count through the merge, the residual axial through-line's fate (a distinct object from the radial charge, reported separately); PLUS the descent-annihilation arm at n = 32 (does plain FIRE annihilate the pair?) and the same-charge released-dynamics spot-check (do cores separate?) | evolution npz + rows + the discriminator verdict |
| P3 | Films (both templates: the capture sequence, the same-charge sequence) + seed/endpoint prints + panel; findings note `m5_21_4_note.md` (method-note grade: this rides the next author outbound) | plots + note |
| P4 | Independent adversarial audit (own energy, own degree, own ledger implementations; tries to REFUTE the sign verdicts, the ledger closures, the annihilation-vs-drain call, the Coulomb fit); FINISH per the flow doc | `m5_21_4_audit.json` |

**The annihilation-vs-drain discriminator (pre-registered)**: ANNIHILATION = the two tracked cores approach and merge in the interior (d(t) → 0 away from the boundary) with the released energy leaving as outward flux (sponge-absorbed) and the total signed degree 0 throughout; DRAIN = charge/energy exits through the boundary with cores never merging (the M5.21.6 B verdict pattern). The two verdicts are never blended; a mixed outcome is reported as mixed.

**Blindspot pass** (unknown unknowns surfaced, routed):

| Blindspot | Route |
| --- | --- |
| Every M5.21-era charge observable (radial alignment a(r), retention) is SIGN-BLIND: an anti-defect is invisible to them | machine-checkable: the signed-degree instrument is new mandatory P0 code, gated on ±1/0/+2 before any pair physics; the audit re-implements it independently |
| Apolar double cover: pointwise sign-alignment of v1 to r̂ around an ANTI-core creates fake discontinuities (v1·r̂ changes sign on a cone) | the instrument orients by CONTINUITY (flood-fill sign propagation on the sphere graph), never by pointwise r̂-alignment; an obstructed lift is itself a measurement, reported not patched |
| A's own through-lines pierce every enclosing sphere (the ½-line structure sits in the v2/v3 sector) | the degree read uses v1 only (orientable where gap12 > 0); sphere radii chosen off the core; gap12 along the sphere checked before each read |
| The relaxed states are NOT compact (r_half tracks the box, M5.21.5): "two separated particles" is approximate at any d | honesty label carried on every E(d) read: defect-core interaction at toy parameters, not point-particle scattering; the Coulomb comparison tests the far-field cross term, the only part superposition predicts |
| The blend-seam trap the angle-additive ansatz avoids could re-enter through the AXIS regularization (e_φ ill-defined at ρ = 0) | reuse the 2b `seed3` axis handling verbatim; the seed gate checks the axis rows explicitly |
| E(d) contaminated by healing-depth mismatch between the pair state and the 2·E_single reference | the reference single-defect runs use the SAME box, BC, and descent depth (the 21.6 audit lesson: cross-task energy comparisons need one scale); depth-sensitivity row at one d |
| Drain-vs-annihilation conflation (the 21.6 lesson) | the pre-registered discriminator above |
| Free-BC antipair may unwind the axial through-line through the boundary and masquerade as annihilation | the through-line's fate is a separate tracked read (loop_read + axis-window winding), never folded into the charge verdict |
| dt stability with two cores + collision speeds | dt = 0.025 certified at 21.6; non-finite backoff armed; the collision window watched at snap cadence |
| Same-charge free release could drain instead of repel | the repulsion read is primary in the PINNED arena (E(d) sign); the released-dynamics run is a spot-check labeled as such |

**Parameters**: δ = 0.3, T2 with the 2b w2, sym stencil, ε = 0. n = 32 (L = 48) workhorse; n = 48 (L = 72) headline film + spot-check. Runtime envelope (from the 21.6/21.5 measurements): n32 equal-depth 1500 it ≈ 3-5 min per run (ladder backgroundable in parallel); n48 evolution 4000 steps ≈ 40 min (background); films minutes. Model/effort: Fable 5 / high (research default). `resets_at` = 14:50 EDT is the RELEASE time, not a deadline: checkpoint eagerly, work normally.

**Research-body destinations**: scripts [`m5_21_4_a_pair.py`](../scripts/m5_21_4_a_pair.py) (seeds + degree + ladder + evolution driver) · `m5_21_4_c_films.py` · `m5_21_4_d_panel.py` · `m5_21_4_audit_check.py` (the audit agent's own); data `../data/m5_21_4_*.json` + npz (heavy arrays local, manifest-listed); plots `../plots/m5_21_4_*.png`; findings [`../findings/m5_21_4_note.md`](../findings/m5_21_4_note.md); checkpoint `../checkpoints/m5_21_4_progress.md`.

**Post-task wiring (user 2026-07-21, recorded at go)**: after this close the sequence is (1) the author outbound carrying BOTH notes (21.5 + 21.4; drafts terminal-only), (2) the high-level doc sweep INCLUDING `MODELS.md` (first touch since the particle-hunt hold) + the M5 model briefing from the hunt results so far, (3) the RENDERING UNLOCK: M5.26 → M5.25 next; M5.22 (nuclei) stays later per the user's sequencing.

## FINDINGS (2026-07-21)

Full method-note record: [`../findings/m5_21_4_note.md`](../findings/m5_21_4_note.md) (equations, code map, gates, tables, films, audit). Headline rows:

| # | Finding | Status |
| --- | --- | --- |
| 1 | P0 gates green attempt 2/3: seed regression exact (6.7e-16), spectrum manifold 1.2e-3 (blend tail, understood), signed-charge suite lands (single +1 / mirror −1 exactly opposite; antipair (+1, −1)/0; same-sector far +2 stable), leapfrog γ = 0 conservation 4.6e-6, sponge monotone; audit exact-sphere: charges integer to 2e-4 | ✅ |
| 2 | NO static separated-antipair regime exists on this instrument: descent annihilates the pair at every ladder d faster than any heal settles it (d = 12 inside 120 iterations); the planned equal-depth E(d) protocol is unrealizable and the depth passes are re-labeled the annihilation scan | ✅ (a negative that redesigned the task) |
| 3 | SEED-LEVEL antipair binding measured: E_int < 0 at all d (attraction, sign unambiguous); within-box ratio to the derived zero-free-parameter Coulomb form −64πc₂/d falls 2.05 → 1.22 with d (n48), but fixed-d ratios move AWAY from 1 with box growth (audit catch): a genuine near-zone excess, local exponent 1.4-1.7; 1/d NOT confirmed at reachable d; near rows blend-sensitive (21-26% with r_c) | ✅ measured, form open |
| 4 | SAME-SECTOR STRING CONFINEMENT: E_int(d) positive and near-linear (slope 7.0/6.2 per unit, two boxes, R² 0.988), ~20× the Coulomb repulsion term at these d: net force pulls like charges TOGETHER; tension value ansatz-grade (cutoff-sensitive 16%, singular winding-2 line persists in the seed, audit catch); the linear FORM is the robust content, topological origin stated at lift level | ✅ form / 🔶 coefficient |
| 5 | THE MERGE: at descent grade all three same-sector seeds collapse into ONE central charge-2 equatorial ring complex (rings at ρ ≈ 14-18; hand cubes: half 15 → +2.085; E annealing 15.0 → 13.7-14.0, d-independent; far +2.07-2.10 conserved): the like-charge compound is ring-shaped, echoing the charged-ring co-candidate and feeding the vortex-knot compound program [M5.22](m5_22_task_details.md) | ✅ |
| 6 | THE CAPTURE AT DYNAMICS GRADE: 4000-step damped-wave run: E 3.87 → 2.24 monotone, 31% absorbed (audit R2: by-difference absorbed = exact discrete damping work to 6.9e-4, the circular-ledger self-catch resolved); per-core charge sigmoid ±0.48 → ≤ 0.005 true residual (audit; half-loss t ≈ 43-47); MECHANISM = CONDUIT ANNIHILATION, audit-confirmed by three independent localizers (>50% of the charge cancels at ~80% of the initial separation; ballistic walk-then-merge refuted; the last ~30% slides inward while dying); interior annihilation per the discriminator (no face transit event); the t = 100 axial flux re-attributed by audit from "debris" to the seed-inherited far-field background, deepened and still growing at the window edge | ✅ audited (R2: 4 confirmed, 1 partial adopted) |
| 7 | Films: descent-fate strips both sectors + the capture sequence, all on the TRUE basic + thermal templates (meridional embed); panel | ✅ |

**The hunt antimatter row consequence**: the remaining item ("the full 3D dynamical capture film") is DELIVERED at verified-L rigor, with the mechanism characterized (conduit annihilation) and the charge + energy ledgers dynamical; the row upgrade lands at REVIEW.

## DATASETS (heavy arrays local-only, gitignored; the folder manifest is `../data/_DATASETS.md`)

| Array | Regen (from `research/scripts/`) | Runtime |
| --- | --- | --- |
| `m5_21_4_lad_{anti,same}_d{12,15,18,24}_n32_it{120,400}.npz` + `_n32.npz` (1500-it, no tag) | `python3 m5_21_4_a_pair.py ladder it=<120\|400\|1500>` | ~40 s/row (120), 2.5 min (400), 4.5 min (1500) |
| `m5_21_4_ev_cap.npz` (11 snapshots, n48) | `python3 m5_21_4_a_pair.py evolve d0=24 n=48 steps=4000 heal=150 tag=cap` | ~59 min |
| `m5_21_4_ev_cap0.npz` (near-raw seed) | same with `heal=25 tag=cap0` | ~59 min |
| `m5_21_4_seed_ladder.json` (tracked, not heavy) | the seed-ladder inline block in the task details § deviations (13:58 row) or re-derive via `seed_pair` + `e_parts` | ~2 min |

## DEVIATIONS LOG

(running; entries added as they happen)

| When | Entry |
| --- | --- |
| 15:25 | AUDIT ROUND 2 LANDED (capture claims; 4 CONFIRMED, 1 PARTIAL adopted, 0 claims refuted, 5 hazards adopted): the ledger fix verified physical to ~0.1% (bit-exact rerun, discrete damping work); the charge endpoint is CLEANER than claimed (≤ 0.005 true vs the 0.02 quadrature floor); conduit annihilation confirmed by three independent localizers with the inward-slide refinement; the "residual line debris" re-attributed to the α-map's seed-inherited far-field background (present at t = 0, deepening not annealing); KE exponent corrected to 1.45; far-bound and per-core mixture caveats added. Note §§ 5, 8 rewritten; FINDINGS row 6 updated |
| 14:2x | AUDIT ROUND 1 LANDED (static claims; 4 CONFIRMED, 2 PARTIAL adopted, 0 refuted): energies reproduced to 4.8e-8, charges shown integer to 2e-4 by exact-sphere reads (the 5-11% was cube quadrature). ADOPTED CORRECTIONS: (a) the Coulomb-approach claim's box direction was WRONG (fixed-d ratios move away from 1 as the box grows: truncation was masking a real near-zone excess; 1/d NOT confirmed at reachable d; near-zone rows are blend-sensitive, 21-26% with r_c); (b) the same-charge seed's escape blend does NOT desingularize the inter-core line (winding-2 in-plane component 0.36-0.71 at ρ → 0): tension re-graded ansatz-grade + cutoff-sensitive (16% with r_t, 12% across boxes, no h-refinement), the linear FORM is the robust content; (c) the topology argument restated at lift level (apolar caveat); (d) lift-sign convention note added to § 3. Note §§ 1, 3, 4, 8 rewritten accordingly. ⚠️ NOTE ON EARLIER ROW LABELS: all times in this log above 13:15 ran ahead of the wall clock; see the checkpoint's 13:58 correction row for the true anchors |
| 14:15 | THE SAME TRAP CAUGHT IN THE CAPTURE ARM: the n = 48 free-BC 800-iteration pre-heal ran the annihilation itself (E 3.43 → 0.70 during heal: FIRE descent IS overdamped attraction, so a deep heal eats the physics the dynamics arm exists to film). Evolve killed and relaunched with heal = 150 (pid 34201): the seed is analytic and on-manifold (GS2 1.2e-3), so only the core-blend transient needs smoothing; the capture belongs to the leapfrog. Also confirmed from the 1500-it scan: anti d = 18 ALSO annihilates by 1500 it (E → 1.054, all fluxes → 0): the descent-grade annihilation scan now brackets d = 12 and 18; d = 24 pending |
| 14:00 | LADDER DEPTH DEVIATION (found on the first row, and it is a result): at d = 12 the antipair ANNIHILATES INSIDE the 1500-iteration heal (all fluxes → 0, on-axis cores gone, tracker reading pin-shell junk at ±18, E dropped 0.90 → 0.71): descent IS overdamped attraction dynamics, so "equal-depth healing" and "stationary cores" conflict at small d. Response: (a) the running 1500-it pass is KEPT and re-labeled the descent-grade annihilation scan (which d survive 1500 it, per sector); (b) a SECOND 400-iteration pass launched for the E(d) curve proper (pid 33244; depth-tagged filenames `_it400`); (c) `core_zs` hardened: boundary margin 4 voxels + the gap-dip VALUE returned so no-core states are detected instead of misread. The stationarity check per DoD applies to the 400-it pass |
| 13:40 | P0 CLOSED attempt 2/3, with one criterion amendment measured into shape: (a) GS1 single-defect regression vs `INS.seed3` exact (6.7e-16); (b) GS2 spectrum-manifold deviation 1.2e-3 at mask d > 10 (attempt 1's 0.010 at d > 8 was EXACTLY the isotropic blend tail, (1 − w)·spectrum-offset at the mask edge: understood, not an error); (c) charge suite: single +1.05 / mirror −1.05 (exactly opposite, the reflection-odd convention lands), antipair (+1.11, −1.12) far 0.001, dynamics gates E-conservation 4.6e-6 rel + sponge monotone. AMENDMENT (logged as deviation): the same-charge candidate's far-sector flux is +2.07/+2.07 (magnitude 2, stable across cube sizes) but its per-core cube fluxes do NOT add up (0.07..0.14 top, 1.0..1.37 bot; 20 orientation conflicts localized at the inter-core escape tube): the k = 2 product ansatz carries its charge partly in the tube channel, so per-core bookkeeping in the same-charge sector is demoted from GATE to MEASUREMENT (tube physics, Faber-flux-tube-like; post-heal re-read reported as found); the sector gate = far flux ±2 stable, which PASSES. Phase conjugated so the sector reads +2 |
| 13:10 | PLAN AMENDMENT (pre-execution, at the seed-math derivation): the "signed degree" gate as first drafted was instrument-theoretically wrong: for apolar tensor fields the oriented degree of ANY smooth 2-defect field with both far sectors sums per the global lift (both pair types read (+1, −1) or obstruct), so a naive ±1/0/+2 degree table cannot distinguish the sectors. The charge of record is the Mermin-Ho B-flux on ONE global continuity-oriented lift (= the M5.21.5 emergent-monopole convention, reflection-odd, the canonical Q lineage). DoD rows 1-2 rewritten before any code: charge suite = per-core flux + far-sphere additivity; same-charge acceptance is MEASURED (+1, +1)/+2, with the α-additive ansatz expected to yield the ANTIPAIR family and a product/stereographic k = 2 azimuthal-winding ansatz staged for the same-charge sector; the attraction/repulsion sign claim moved to reference-free dE/dd |

## TASK REVIEW (2026-07-21)

**Task Duration:** 2:30 (from ~13:15 to 15:45 EDT)
**Usage Cap Triggered:** NO (the 14:50 watchdog fired with the session alive; the ping was pushed forward and parked at FINISH without firing)

Approved by the user 2026-07-21 (terminal review).

**Results**: P0 gates green attempt 2/3 (signed-charge instrument lands; audit exact-sphere: charges integer to 2e-4) ✅ · NO static separated-antipair regime (descent annihilates faster than any heal; the E(d) protocol re-designed to seed level) ✅ · seed-level antipair binding measured, outer-window trend consistent with the derived zero-free-parameter Coulomb form, 1/d not confirmed at reachable d (audit box-direction catch adopted) ✅ · SAME-CHARGE STRING CONFINEMENT (linear E_int, slope 7.0/6.2 two boxes, ~20× Coulomb; merge into a central charge-2 ring complex) ✅ form / 🔶 coefficient · THE CAPTURE FILM at dynamics grade with CONDUIT ANNIHILATION audited by three independent localizers, charge sigmoid ±1 → ≤ 0.005, ledger physical to 0.1%, interior-annihilation verdict, cap0 robustness ✅ · films both TRUE templates (4 sequences) + panel ✅ · adversarial audit two rounds: 8 CONFIRMED / 3 PARTIAL adopted / 0 claims lost ✅.

**Issues / blockers**: the 1/d far-form needs boxes beyond n48; the string tension needs h-refinement before any beyond-form quote; the α-map seed threads a background flux through the whole box (clean-vacuum endpoints need compactified far fields); the same-sector story is axisymmetric-sector-specific.

**Deviations from plan**: five, logged live in § DEVIATIONS LOG (charge-instrument plan amendment pre-code; the heal-eats-the-physics catch in both arms; the ladder re-labels + seed-level redesign; two audit-adoption rounds; the timestamp correction).

**Action needed** (executed at this closeout): roadmap Done append + the RENDERING UNLOCK marker tripped; hunt antimatter row upgraded; canonical recipes/anti-recipes + briefing arc updated. Next per the user's stated sequence: the author outbound carrying BOTH notes (21.5 + 21.4), the high-level doc sweep including `MODELS.md` + the model briefing from the particle-hunt results, then the rendering tasks (M5.26 → M5.25).

**Findings**: The antipair capture-to-annihilation film is delivered and audited at verified-L rigor, revealing the mechanism: charges cancel through the connecting topological line (conduit annihilation), not by cores walking together. The like-charge sector shows string confinement instead of two-body repulsion (linear E(d), two box sizes) and merges into a single charge-2 ring compound, landing directly on the nuclei-as-vortex-knots doorstep ([M5.22](m5_22_task_details.md)). The seed-level antipair binding is consistent with the derived zero-free-parameter Coulomb form as an outer-window trend, with an honest unresolved near-zone excess.

**Research docs created / updated**: [`../findings/m5_21_4_note.md`](../findings/m5_21_4_note.md) (the method note) · this file · scripts [`m5_21_4_a_pair.py`](../scripts/m5_21_4_a_pair.py) · [`m5_21_4_c_films.py`](../scripts/m5_21_4_c_films.py) · [`m5_21_4_d_panel.py`](../scripts/m5_21_4_d_panel.py) · [`m5_21_4_e_post.py`](../scripts/m5_21_4_e_post.py) · [`m5_21_4_audit_check.py`](../scripts/m5_21_4_audit_check.py) (the audit agent's) · data gates / ladder passes / seed ladder / cap + cap0 rows + post + zprof / audit + `_DATASETS.md` · plots [`m5_21_4_panel.png`](../plots/m5_21_4_panel.png) + 8 films · [`../m5_roadmap.md`](../m5_roadmap.md) · [`../m5_particle_hunt.md`](../m5_particle_hunt.md) · [`../m5_theory_canonical.md`](../m5_theory_canonical.md) · [`../../__M5_model_briefing.md`](../../__M5_model_briefing.md) · [`../checkpoints/m5_21_4_progress.md`](../checkpoints/m5_21_4_progress.md)
