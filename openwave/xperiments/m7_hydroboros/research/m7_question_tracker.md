# M7 / HydroBoros, question tracker + hardest pieces

**Purpose:** single source of truth for (a) M7's open research questions (the REGISTRY: every question carries a stable ID, `Qn`, never renumbered), (b) the resolution chronology, and (c) the long-running **hardest-pieces** board. Mirrors the [M5 tracker pattern](../../m5_liquid_crystal/research/m5_question_tracker.md). Update this doc when a question opens, gets sent to a collaborator, gets answered, demoted, or resolved. **The [`§ OPEN QUESTIONS`](#open-questions) table IS the ask list**: it is sorted by priority (row order = most critical first = the order to raise them), and the primary audience is **Marc Fleury** at the next contact (Werbos for the M6-calibration items, tagged in the count block). Per the comms rule, this file supplies the factual content; Rodrigo phrases every outbound message.

**Sister docs:**

| Doc | Purpose |
| --- | --- |
| [`m7_roadmap.md`](m7_roadmap.md) | the task roadmap (M7.0-M7.17); where each question resolves |
| [`m7_background.md`](m7_background.md) | full implementation background: the two physics parents, the M5 method, the dynamics (§ 5) |
| [`m7_theory_canonical.md`](m7_theory_canonical.md) | the canonical build-spec (landed at the M7.7 milestone) + the staged column preview [`preview_models.md`](preview_models.md) |
| [`tasks/`](tasks/) | per-task detail docs (`m7_N_*.md`) |
| [`../../m5_liquid_crystal/research/m5_question_tracker.md`](../../m5_liquid_crystal/research/m5_question_tracker.md) | the M5 tracker this one mirrors (structure + procedure) |
| [`../../m6_ouroboros/research/0b_question_tracker.md`](../../m6_ouroboros/research/0b_question_tracker.md) | the M6 tracker (the other parent's registry) |

---

## Active Questions Count

```text
5 ASK MARC     (2026-07-06 call; list order = priority = the table order;
                pre-call working notes received 2026-07-05, audited: the
                core math CHECKS OUT, see the Qn details. Q15 promoted to
                the top 2026-07-05: it is the call's primary decision,
                together with the M7.8 run/no-run)
                 Q15  UNITS CONTRACT: the notes pin Case B (omega = omega_C;
                      spin = the helicity-pair asymmetry (U+ - U-)/omega =
                      hbar/2) and EXCLUDE the 2 omega_C pinning on the tail
                      budget; the 2 omega_C Zitter survives in the
                      bilinears (where our own 2-omega observation lives);
                      pre-call notes, unvetted by the author: needs his
                      live confirmation; M7.8 = the lattice test
                 Q7   charge-carrying construction: reframed AGAIN by the
                      notes' no-go theorem (a localized oscillating interior
                      carries NO net charge; net charge = an asymptotic
                      datum, the Coulomb tail living exactly where
                      lambda -> 0): the interior-j0-profile ask is
                      superseded by the tail-matching + tail-quantization
                      program; our M7.6 fixed-reservoir architecture = the
                      lattice version of the imposed tail
                 Q3   divergence charge vs linking charge: M7.4 measured
                      them INDEPENDENT; the notes agree structurally (charge
                      rides the lambda-gradient, helicity rides the winding)
                      AND offer the deeper slaving as the tail-quantization
                      conjecture (interior winding fixes e) - confirm at
                      the call
                 Q1   substrate reading + target manifold (S2 vs S3);
                      A-primary + phi = 0 reconfirmed by the notes
                 Q4   mostly delivered (the notes ARE the promised
                      material); residual = the Spanish-school logistics

2 ASK WERBOS   (posture since 2026-07-05: deliver results first; when
                asking, ask for the smallest possible unit of authorial
                input, one item per contact; the first round-trip brought
                no resolutions, and these two are the only asks that are
                author-gated by nature: intent + definitions)
               Q12  ODE provenance: 0d_canonical 2.2's "2 omega alpha" form
                    is not an EL reduction; LoE 9b 5.1's cos/sin phases
                    decouple - which ansatz/ODE is intended? (+ the Q6
                    residual: benchmark sign slip + the (g/4) doc fix)
               Q9   (Omega, G) definitions: table + k self-read from corpus
                    #10 at M7.4 (electron Omega=1.050, G in [1.15,3.25],
                    k>0); G-island does NOT bracket g=1.0625, so
                    (Omega,G) != (omega,g); the micro-ask = ONE verified
                    electron-island point in (omega, g, lambda, m_J);
                    M7.5 offers a candidate anchor: his stability islands
                    may be the omega > omega* region

3 SELF-DETERMINE (re-scoped from ask-Werbos 2026-07-05 after the first
                  round-trip: no resolutions arrived, and all three are
                  physics/measurement questions the lattice can answer
                  in-model during Phase 2; results get SHARED, not asked)
               Q14  THE VACUUM TACHYON (M7.5): det M(0) = -1 for ANY f:
                    unconditional long-wavelength instability (measured
                    0.785 vs analytic 0.786; no v5 beta* threshold in the
                    vector truncation). Self-route: linearize WITH the
                    scalar/Gauss sector on our own lattice (does it lift
                    det M(0)?); second candidate = the variable-lambda(r)
                    mass profile (massless exterior, via the 2026-07-05
                    Fleury notes); pre-step to M7.12 annihilation
               Q11  the charged H/Q = 1.6890 is a WINDOWED quantity (M7.3).
                    Self-route: parameter scan for a genuinely localized
                    charged branch (rides the M7.13 islands task)
               Q13  the M6 electron is a 3D constrained SADDLE; helicity
                    stabilizes the SAME torus (M7.4 measured fix); solitons
                    exist only above omega* = 0.786 (M7.5). Self-route:
                    the physics is measured; the residual 1D-conjugate-
                    point provenance rides with Q12

5 RESOLVED     Q10  FLDB energy convention: RESOLVED 2026-07-04 (author
                    email): the Eq 122/124/127 bug CONFIRMED (caught
                    internally, the update never pushed); author's corrected
                    figure 0.95 vs our 0.958 (~1% gap, inside the thin-torus
                    approximations); M7.2 vindicated
               Q8   gauge/constraint scheme (M7.3: no gauge fixing; the fix
                    was the objective: fixed-Q_can extremization)
               Q2   4th-order stabilizer NOT needed (M7.4: helicity +
                    confinement stabilizes; Derrick interior min measured)
               Q5   div F does NOT destabilize (M7.4: the divergence-ful
                    Hopf seed relaxes to a stable soliton); residuals:
                    knot-SECTOR persistence (reconnects under global-H
                    fixing) + the net-Gauss/scalar-sector version
               Q6   f(J.J) form: RESOLVED-empirical at M7.4/M7.5: the
                    program runs the REPULSIVE/written f = gs^2 (only
                    branch with stable solitons; LoE v5's own text; and
                    M7.5 showed both conventions vacuum-tachyonic, so no
                    physics is pending on the choice); residual = Werbos's
                    intent confirmation + the 0d_canonical (g/4) doc fix,
                    folded into Q12 (provenance)

Total: 15 questions (5 ask-Marc, 2 ask-Werbos, 3 self-determine, 5 resolved).
```

Legend: 🔶 open, direction known / partial · 🚧 open, not yet started · ✅ resolved · ❌ closed negative.

---

## OPEN QUESTIONS

> Sorted by priority: most critical first; **row order = the order to raise them with Marc**. Full statement + provenance + history + evidence per question: [`§ QUESTION DETAILS`](#question-details-open-questions) (one anchor per ID).

| ID | Title | Question (one line) | Gates |
| --- | --- | --- | --- |
| [Q15](#q15-detail) 🔶 | The units contract (NEW at M7.7) | The measured `j_z = 1` per quantum and `ωL_z/E = 2.07` need ONE mapping choice: `ω = ω_Compton` vs `ω = ω_Dirac` (Zitter, our on-record recommendation). **The Fleury working notes (2026-07-05) pin Case B: `ω = ω_C`**, with spin ℏ/2 read from the helicity-pair asymmetry `(U₊ − U₋)/ω` (not total `L_z`), and **exclude** the `2ω_C` pinning on the tail budget; the `2ω_C` Zitter survives in the bilinears. Pre-call notes, unvetted: needs live confirmation; **M7.8 is the lattice test** (`U₊/U₋ = 3 + α/2 + 4f_bb`) | [`m7_theory_canonical.md § 4`](m7_theory_canonical.md); gates the M7.15 mass/spin cells; M7.8 |
| [Q7](#q7-detail) 🔶 | Charge-carrying construction | Reframe VALIDATED at M7.4; M7.6 self-resolved the fixed-reservoir prescription (Gauss 99.1%, `1/d` reference-matched, 1.17 dressing); **reframed AGAIN by the Fleury working notes (2026-07-05)**: a localized oscillating interior carries NO net charge (no-go theorem); net charge is an **asymptotic tail datum** living where `λ → 0`, and closure = interior-exterior matching + a tail-strength quantization (open on his side too: the "α computable" prize). The interior-`j₀`-profile ask is superseded; call ask = the matching program + what fixes `e` | the self-consistent charged soliton; Q14 |
| [Q3](#q3-detail) 🔶 | The two charges | M7.4 measured them **independent at this level**; the Fleury notes **agree structurally** (charge rides the λ-gradient, helicity rides the winding: different carriers) and supply the deeper-slaving candidate Q3 asked about: the **tail-quantization conjecture** (interior winding/helicity fixes the exterior tail strength `e`), proposed, unproven | interpretation; the tail-quantization conjecture |
| [Q1](#q1-detail) 🔶 | Substrate + target manifold | The `(A_μ, J_μ)` doublet (candidate B, built at M7.1) vs single RS field (A); Clebsch as seeder only (D); and the target manifold: Pisello S² vs Faber S³? (the 2026-07-05 notes reconfirm A-primary, `φ = 0`, real trig; silent on S²-vs-S³) | M7.3/M7.4 closure |
| [Q4](#q4-detail) 🔶 | Marc's source material | Mostly delivered 2026-07-05: the working notes ARE the promised Beltrami material (persisted to [`../theory/electron_beltrami/`](../theory/_CITATIONS.md)); residual ask = the Enciso & Peralta-Salas (Spanish school) logistics | corpus (M7.0, ongoing) |
| [Q14](#q14-detail) 🔶 | The vacuum tachyon (NEW at M7.5) | The linearized transverse vacuum of the written theory has `det M(0) = −1` for ANY `f`: an **unconditional long-wavelength tachyonic band** (`k² < 0.618` repulsive / `1.618` focusing; measured growth rate 0.785 vs analytic 0.786, amplitude-independent: **no `β*` threshold exists** in the vector truncation, v5 Test 1 fails there). The harmonic frame survives because ω supplies the stiffness (PSD iff `ω ≥ ω* = 0.786`; soliton existence threshold **measured**, bracketed 0.75-0.79). What cures the vacuum in the full model: an A-mass from a condensate vacuum, the scalar/Gauss sector, a finite-universe IR cutoff, or parameter islands? (NEW candidate via the Fleury notes 2026-07-05: a spatially-varying mass `λ(r) → 0` outside the soliton, leaving a massless, healthy exterior vacuum) **SELF-DETERMINE since 2026-07-05**: the cure check is computable in-model (linearize with the scalar/Gauss sector; test the variable-λ profile); results shared, not asked | M7.5 delivered the evidence; blocks ANY real-time program (M7.12 annihilation) until answered or cured; Phase 2 pre-step |
| [Q11](#q11-detail) 🔶 | Windowed charged calibration | M7.3 showed `H/Q = 1.6890` is a windowed quadrature (no decaying channel at `ω=1, λ=1`; `Q` grows with `r_max`): is the calibration window-defined, or does a genuinely localized charged branch exist elsewhere in parameter space? **SELF-DETERMINE since 2026-07-05**: answered by a parameter scan, not an ask (rides the M7.13 islands task) | M7.3 delivered the evidence; blocks any physical-mass reading of the M6 charged ledger |
| [Q13](#q13-detail) 🔶 | 3D stability of the chaoiton | M7.3: the M6 electron is a 3D constrained SADDLE of its verbatim functional, ending in focusing collapse (helicity guard inert on it): is the conjugate-point stability claim (LoE 9b § 5) restricted to the 1D radial manifold? M7.5 sharpened: solitons exist only above `ω* = 0.786` (the clock IS the stabilizer); the real-time part subsumed by Q14 **SELF-DETERMINE since 2026-07-05**: the physics is measured in-model; the residual 1D-conjugate-point provenance question rides with Q12 | M7.5 delivered the threshold; provenance residual → Q12 |
| [Q12](#q12-detail) 🔶 | M6 ODE / ansatz provenance | The `0d_canonical § 2.2` `2ωα` form is not an EL reduction of the Lagrangian under any scanned harmonic ansatz, and LoE 9b § 5.1's cos/sin phases decouple: which ansatz + ODE does Werbos intend as canonical? Now also carries the Q6 residual: confirm the benchmark sign slip + `f = gs²` (separate λ) as canonical, and the `0d_canonical` `(g/4)` doc fix | M7.3 delivered the reduction table; documentation fix for `0d_canonical.md`; absorbs the Q6 residual (2026-07-03) |
| [Q9](#q9-detail) 🔶 | Werbos-v5 calibration dictionary | Partially self-read at M7.4 (corpus #10 table: electron `Ω = 1.050, G ∈ [1.15, 3.25], k > 0`; `k` = far-field decay): the `(Ω, G)` **definitions** remain the ask: the electron G-island does not bracket `g = 1.0625`, so `(Ω,G) ≠ (ω,g)`; needed to locate the claimed localized branch (Q11) | M7.13 islands · Q11 |

---

## RESOLVED QUESTIONS

| ID | Question | Resolution |
| --- | --- | --- |
| [Q10](#q10-detail) ✅ | FLDB energy convention | RESOLVED 2026-07-04 by the author (email reply to the Phase 1 report): the Eq 122/124/127 bug is **confirmed** (caught internally after publication; the update was never pushed, so the stale figure stayed in the paper); the author's corrected figure is **0.95** vs our independently-computed **0.958** (the ~1% gap sits inside the thin-torus approximations). M7.2's reproduction and the corrected convention stand. [`tasks/m7_2_fleury_torus.md § 3`](tasks/m7_2_fleury_torus.md) |
| [Q8](#q8-detail) ✅ | Gauge / constraint scheme for the coupled minimizer | RESOLVED at M7.3 (2026-07-03), as scheduled: **no explicit gauge fixing**. The coupled fixed-`Q_can` relaxation ran gauge-unfixed with no stall or drift pathology; the correction the pre-gate actually forced was **objective-level**, not gauge-level: minimize/extremize `E_ω` at fixed `Q_can` (multiplier ω), never `E_ω` unconstrained (its EL has the ω²-sign flipped). M7.1's G5 evidence (curl sector self-fixes; only `A·J` gauge-sensitive off-shell) stands. Residual: if a future M7.4+ run shows gauge stall, reopen with the Coulomb-projection candidate. [`tasks/m7_3_ouroboros_3d.md § 1, § 4`](tasks/m7_3_ouroboros_3d.md) |
| [Q2](#q2-detail) ✅ | 4th-order stabilizer needed? | RESOLVED at M7.4 (2026-07-03): **not needed**. Helicity anti-collapse + Ouroboros confinement stabilizes with no 4th-order term: every surviving M7.4 state has a constrained-Derrick **interior minimum** (dilation probe, measured), and both zero-helicity seeds evaporate (the guard is real and load-bearing). The drafted `\|F×(∇×F)\|²/\|F\|²` term stays retired (inert on Beltrami configs + singular at zeros). [`tasks/m7_4_charged_soliton.md § 2`](tasks/m7_4_charged_soliton.md) |
| [Q5](#q5-detail) ✅ | Divergence-ful knots | RESOLVED-delivered at M7.4 (2026-07-03): **`∇·F ≠ 0` does NOT destabilize**: the divergence-ful Bateman/Hopf seed relaxes to a stable, dilation-stable, approximately-Beltrami soliton with persistent RMS charge (`Q_ρ = 5.4e-3`). Residuals routed: (a) **knot-SECTOR persistence**: the final state reconnects into the same Taylor family as the unknot seeds (only global `H_A` was fixed): topology-preserving constraints are a designed follow-up; (b) the **net-Gauss-charged** version (scalar sector) pends the Q7(b) prescription. [`tasks/m7_4_charged_soliton.md § 2-2b, § 4`](tasks/m7_4_charged_soliton.md) |
| [Q6](#q6-detail) ✅ | Potential form | RESOLVED-empirical at M7.4/M7.5 (2026-07-03): **the program runs the repulsive/WRITTEN `f = gs²`** (λ separate): the only branch with stable 3D solitons that keep charge (M7.4 matrix), LoE v5's own written form, and M7.5 removed the last physics stake (BOTH conventions are vacuum-tachyonic, bands 0.618 vs 1.618, so the choice cannot cure Q14 either way). No physics is pending on Werbos's answer. Residual routed to **Q12** (provenance): confirm the benchmark sign slip + the `0d_canonical.md` `(g/4)` transcription fix. [`tasks/m7_4_charged_soliton.md § 2b`](tasks/m7_4_charged_soliton.md) · [`tasks/m7_5_clock_stability.md § 2`](tasks/m7_5_clock_stability.md) |

---

## HARDEST PIECES, status board (risks / unknowns)

The long-running load-bearing unknowns, distinct from the discrete Q-numbered questions above (a piece can persist across many tasks). Promote a row to a numbered question once it becomes a decision the build must make.

| Hardest piece | Status / mitigation |
| --- | --- |
| The time-harmonic reduction goes subtly wrong (M6 needed ten sandbox versions to pin its 1D reduction: signs, regularity classes, Laplacians, measures) | ✅ CLOSED at M7.3: the verbatim-ODE pre-gate passed with every sign/normalization pinned by sympy (and it caught three real convention faults: the coupling sign, the focusing `f`, the wrong objective `E_ω`-unconstrained), [`tasks/m7_3_ouroboros_3d.md § 1`](tasks/m7_3_ouroboros_3d.md) |
| The 1D chaoiton may be **unstable to 3D symmetry breaking** (1D radial solutions often are) | ✅ ANSWERED at M7.3, honestly: it is a 3D constrained SADDLE with a focusing-collapse escape (Q13); feeds the M7.4 blend design (the helicity guard is inert on M6's ansatz; the blend activates it) |
| Gauge flat directions stall or misdirect the minimizer (Q8) | ✅ RESOLVED at M7.3: no gauge fixing needed; the coupled fixed-`Q_can` relaxation ran gauge-unfixed cleanly (see Q8 resolution row) |
| Confinement-term sign: `m_J² A·J` is not positive-definite, the anti-expansion balance is only empirically established (M6 1D) | ✅ ANSWERED at M7.4: the dilation probe measured a constrained-Derrick **interior minimum** on every surviving state (repulsive `f`); the balance holds. Residual trail: M7.5 identified the scalar runaway as one face of the **unconditional vacuum tachyon** (promoted to **Q14**); M7.6 **self-resolved the working prescription** at the fixed-reservoir level (`j₀` fixed + `a₀` free: Coulomb landed); the self-consistent charge remains with Q14 |
| **The vacuum tachyon (Q14, opened at M7.5): the load-bearing theory unknown** | the linearized vacuum is unconditionally unstable at long wavelength (`det M(0) = −1`, any `f`; measured 0.785 vs 0.786); the harmonic frame is immune above `ω* = 0.786` (the clock IS the stabilizer, threshold measured), so **Phase 1 (the harmonic program) is unaffected**, but any real-time program (M7.12 annihilation) is blocked until the full model's cure is known; SELF-DETERMINE since 2026-07-05 (Phase 2 computes the scalar-sector linearization + the variable-λ profile in-model) |
| Variable-λ Beltrami math "gets very hairy" (Marc), now sharpened: **rigidity obstructs exact nonconstant-λ solutions even locally** | ✅ reframe VALIDATED at M7.4: relax-and-measure produced approximately-Beltrami solitons (`\|align\| = 0.96`) with the charge measured, never imposed |
| Does the divergence-ful field admit clean knots (Q5) | ✅ ANSWERED at M7.4: yes (see Q5 resolution); M5.11's P2 expansion mode did not appear: the confinement + helicity pair held |
| Whole-program cost: 3D harmonic relaxations are multi-hour GPU runs | accepted; same regime as M5.11's vortex-loop relaxation |
| **Marc's AI-exchange material hallucinates** (his own warning: 2 months / 3 AIs, output not great) | treat it as **seeds only**, never as validated input; every claim re-derived in-platform (the MODELS.md reproducibility bar). A marathon-session review for extractable signal is optional, low priority |
| Masses (M7.6) may stay in tension with data | first numbers in (M7.6): the spin quantum is CLEAN (`j_z = 1` to 0.6%); mass = 6.3246 program units pending the units-contract decision (M7.7); μ charge-unit-blocked (scalar sector); reported honestly per the M5.11 pattern |

---

## Notes on scope

- This tracker covers **M7 physics/framework questions for the collaborators** (Marc Fleury primary; Paul Werbos for the M6-calibration items) plus the hardest-pieces board. Implementation decisions (lattice layout, kernel design, minimizer settings) are tracked as roadmap tasks in [`m7_roadmap.md`](m7_roadmap.md) and the per-task docs, not here: the same split as M5/M6.
- **Outbound comms:** the [`§ OPEN QUESTIONS`](#open-questions) table order = the ask order at the next Marc contact; the [`§ QUESTION DETAILS`](#question-details-open-questions) entries carry the content bullets for each ask. The agent supplies facts; **Rodrigo phrases every outbound message** (his voice is the credential).
- **Comms plan (2026-07-02, deliver-first-ask-second, the M5.16 pattern; updated 2026-07-03 night):** run through the **M7.7 milestone** without waiting on answers (per-task dependency check: nothing external blocks M7.5-M7.7), then the big stop: both packages go out with the MODELS.md column behind them. **Marc package** (Q7, Q3, Q10, Q1, Q4) backed by M7.2-M7.4 + M7.6 (the rotating `j_z = 1` electron, the Coulomb sector, the 1.17 dressing); **Werbos package** (**Q14 first**, Q11, Q13, Q12 incl. the Q6 residual, Q9) backed by M7.3-M7.6 (the reduction table, the window plot, the tachyon dispersion + measured rates, the ω\* threshold scan, the RKKY consequence of the delocalized channel). Pre-agreed tripwire: if a task hits a question we cannot self-resolve (e.g. the scalar/Gauss prescription blocking Coulomb at M7.6), stop and ask THEN. Both packages follow the mandatory [`METHOD_NOTE.md`](../../../../dev_docs/METHOD_NOTE.md) standard (2026-07-03): equations first, commit-pinned equation-to-code map, small auditable physics module (the M7.7 consolidation extracts it), results after methods, explicit not-computed list. **Status (2026-07-05): the Marc package went out 2026-07-04** (the [Phase 1 report](tasks/m7_phase1_report.md) + the Q7/Q3/Q10/Q15/Q1/Q4 asks inline); the same-day reply **resolved Q10**, and pre-call working notes followed 2026-07-05 (routed to Q7/Q3/Q15/Q14/Q1/Q4; see the Qn details); a **live call is set for 2026-07-06** (agenda = the open Marc asks + the M7.8 run/no-run decision; agenda sheet: [`tasks/m7_7_call_prep.md`](tasks/m7_7_call_prep.md)). **The Werbos package also went out 2026-07-04**; initial replies arrived the same day with **no question resolutions and no actionable provenance input** (limited bandwidth flagged on his side; no rush accepted). **Re-scope (2026-07-05):** Q14/Q11/Q13 moved to **SELF-DETERMINE** (physics/measurement questions the lattice answers in-model during Phase 2; results shared, not asked); Q12/Q9 stay ask-Werbos but **micro-scoped** (one smallest-possible unit of authorial input per contact; for Q9 that is a single verified electron-island point). Outbound posture with this channel = **deliver results first**. A second relayed message (2026-07-05 night) carried an unverified localization claim; it was **tested and refuted in-repo the same night** (Q11 micro-slice) with **no standalone reply sent, by design**: the result is documented under [Q11](#q11-detail) and rides the next scheduled delivery. **Public record note (2026-07-05):** a preprint (Zenodo record 21211281) cites this repo (its ref [9]) and attributes to M7 a window-locked charge `Q = 1.6889` across `L = 32-128`; the repo's **measured result is the opposite** ([Q11 detail](#q11-detail): window-divergent for every `(ω, g)` tested, and `1.6889` is the `H/Q` ratio, not a charge); it also describes our M7.3 ledger reproduction as an independent physical mass/g-factor validation, which it is not (code-to-code, model units, window-defined). An on-list factual clarification, scoped strictly to the two statements describing this repo's results, with a v2 replacement-wording offer, went out 2026-07-05.

---

**Last updated:** 2026-07-05 night (**Q11 micro-slice, external claim refuted + localization boundary found**: an externally reported sweep (unverified, no script/definitions) claimed a localized charged branch at `ω = 0.8, g ∈ {0.25, 0.5}`; the one-hour falsification run ([`scripts/m7_13_q11_window.py`](scripts/m7_13_q11_window.py)) tested `(ω ∈ {0.8, 1.0}) × (g ∈ {0, 0.25, 0.5, 1.0})` over windows 8-128 on the benchmark quadratures: **all DIVERGENT, `Q(2W)/Q(W) ≈ 2`**, `g` amplitude-rescales but never localizes (the far-field dispersion carries no `g`), none of the quoted numbers reproduce. Bonus: the decaying-channel condition at the canonical `λ = m_c = 1` is **`ω < 0.786 = ω*`**: localization and existence are mutually exclusive in the vector truncation; the M7.13 branch hunt must move `(λ, m_c)`, not `(ω, g)`. Q11 stays SELF-DETERMINE; no baseline change.) Earlier (**Werbos round-trip + re-scope**: the Werbos package (sent 2026-07-04 with the [Phase 1 report](tasks/m7_phase1_report.md)) got its first replies the same day: **no question resolutions and no actionable provenance input** (limited bandwidth flagged; our "no rush" accepted). Consequence: **Q14/Q11/Q13 re-scoped to SELF-DETERMINE** (all three are physics/measurement questions the lattice answers in-model during Phase 2: Q14 = linearize with the scalar/Gauss sector + test the variable-λ mass profile, the M7.12 pre-step; Q11 = the localized-branch parameter scan, rides M7.13; Q13 = physics already measured, provenance residual folded into Q12); **Q12/Q9 stay ask-Werbos, micro-scoped** (one smallest unit of authorial input per contact; Q9's = a single verified island point). Outbound posture = deliver results first. Count: 5/5/0/5 → **5/2/3/5**. Companion doc: the repo-root AI-collaboration hygiene guide (`AI_HYGIENE.md`) opened the same day.) Earlier same day (**Marc round-trip + working notes**: the Marc package went out 2026-07-04 with the [Phase 1 report](tasks/m7_phase1_report.md); the same-day author reply **RESOLVED Q10** (the Eq 122/124/127 bug confirmed, caught internally and never pushed; corrected figure 0.95 vs our 0.958). On 2026-07-05 two pre-call working-note documents arrived (the thin-torus ansatz recommendation + the closure results), persisted **local-only** to [`../theory/electron_beltrami/`](../theory/_CITATIONS.md) per the corpus provenance rule, and **audited at receipt** (the LG closure relations, the tail integral `U_tail = (α/8)m_ec²`, and the corrected 3:1 algebra re-derived in-session: the core math checks out; the postulates P1-P3, the harmonic λ² profile, and the secular-tail reading are the assumed parts). Content routed: **Q7** reframed (no-go theorem: net charge = an asymptotic tail datum; the interior-`j₀` ask superseded by tail matching + tail quantization), **Q3** gains the tail-quantization slaving candidate, **Q15** gains the Case-B pinning (`ω = ω_C`, spin = pair asymmetry; Case A/Zitter excluded on the tail budget; pre-call, unvetted), **Q14** gains the variable-λ cure candidate (massless exterior), **Q1/Q4** addenda (A-primary reconfirmed; the promised material delivered). **NEW task M7.8** (the helicity-pair `U₊/U₋` test, prediction `3 + α/2 + 4f_bb`) added at the top of the Phase 2 backlog ([roadmap](m7_roadmap.md)); run/no-run + scope at the **2026-07-06 call**. Count: 6/5/0/4 → **5/5/0/5**.) Earlier (**M7.7 MILESTONE delivered**, [`tasks/m7_7_consolidation.md`](tasks/m7_7_consolidation.md): Phase 1 consolidated: the canonical spec [`m7_theory_canonical.md`](m7_theory_canonical.md) (equations first, METHOD_NOTE-shaped), the ~200-line physics module `m7_functional.py` with the one-script reproduction `m7_7_canonical.py` (**all gates first-try at N = 64 AND N = 48; engine-vs-reference cross-validation 1.4e-14**), and the 21-cell HydroBoros column drafted honestly at 0 ✅ / 8 ⚠️ / 13 🚧 and **STAGED as a preview in [`preview_models.md`](preview_models.md)** (review decision: the research is not yet ready for the cross-model benchmark; MODELS.md untouched; entry deferred to the M7.15 governance step). Both comms packages prepared as content bullets in the task doc § 3 (W: Q14-led; M: Q7-led), governance issue text drafted for later; permalink pinning + commit = Rodrigo's actions at the milestone stop. No question resolutions this task (consolidation); count stays 5/5/0/4.) Earlier (**M7.6 delivered**, [`tasks/m7_6_observables.md`](tasks/m7_6_observables.md) § FINDINGS: in the M7.5 frame (fixed `Q_can` + `H_A`, rotating seeds) the **rotating electron candidate landed**: blend_m1 relaxes to a stable localized soliton (E = 6.3246, `\|g\| = 1.6e-7`) that is a clean **`j_z = 1` per-quantum rotating wave (0.994, both sectors)** with the energy budget closing exactly; **Coulomb landed** via the self-resolved fixed-reservoir prescription (`j₀` fixed + `a₀` free: stable, Gauss flux 99.1%, far-field −2.14, two-charge splitting reference-matches Poisson-in-box at a constant **1.17 dressing**: Q7(b) partially self-resolved, tripwire NOT fired); the neutral-pair channel shows an **oscillatory RKKY exchange** (period `π/k`, the Q11 tail mediating forces); KG: both dispersion branches exact KG form, `m_eff² = φ`; zero-helicity states **delocalize** to the band-edge condensate in this frame (vs evaporating at fixed `H_A`); μ measured de-phased (36.5 p.u.) but the `μ_B` comparison is charge-unit-blocked; ℏ/2-vs-ℏ = units-contract decision → M7.7. No new resolutions beyond Q7's partial (count unchanged 5/5/0/4).) Earlier (**M7.5 delivered**, [`tasks/m7_5_clock_stability.md`](tasks/m7_5_clock_stability.md) § FINDINGS: the real-time Minkowski leapfrog validated the translation exactly (`⟨E_real⟩ = E_ω` to 1.85e-14, O(dt²) conservation) and then found **Q14, the vacuum tachyon**: `det M(0) = −1` for any `f`, unconditional long-wavelength instability (measured growth rate 0.785 vs analytic 0.786, amplitude-independent: **no v5 `β*` threshold exists** in the vector truncation); the harmonic frame is immune because the clock rides above the band (`E_ω` PSD iff `ω ≥ ω* = 0.786`), and the fixed-`H_A` ω scan **confirmed the predicted existence threshold** (runaway at 0.70/0.75, solitons from 0.79, `Q_J` diverging toward ω\*) while passing the Legendre gate `dE*/dω = Q_can` to ~1-2%: ω and `Q_can` verified conjugates in 3D (the de Broglie clock's correct frame). Two more structural findings: the M7.4 winner is a **standing-wave doublet** (`a_s = j_s = 0`: the rotating sector was never explored → M7.6 seeds rotating pairs) and its real-time residual decomposes as `α = −1.978 ≈ −2ω²` + helicity-multiplier term (`μ = −0.393 ≈ H_cross/H_A`), with 3ω leakage only 0.2%: **the reduction error is the constraint FRAME (fixed `H_A` vs fixed `Q_can`), not the ansatz**. Q13 sharpened (the clock IS the stabilizer), Q11 unified (one dispersion carries the delocalization and the tachyon), Q9 gains a candidate anchor (islands ↔ ω > ω\*), Q6 scoped (f-choice cannot cure the vacuum). Comms plan updated: run to the M7.7 milestone, then both packages, METHOD_NOTE-compliant, Q14 leading the Werbos package.) Earlier same day (**M7.4 delivered**, [`tasks/m7_4_charged_soliton.md`](tasks/m7_4_charged_soliton.md) § FINDINGS: the seed × f-convention matrix produced **three stable, finite-size, approximately-Beltrami solitons** (ck unknot, Hopf knot, and the **blend** = M6 torus + poloidal A-twist, the HydroBoros electron candidate), all `\|align\| = 0.96`, dilation-stable, with a coupling-driven J-condensate and persistent RMS divergence charge; both zero-helicity parent seeds evaporate (helicity measurably load-bearing). **Q2 RESOLVED** (no 4th-order term needed), **Q5 RESOLVED-delivered** (divergence-ful knots hold; residual = the net-Gauss/scalar-sector version), **Q3 evidence** (the two charges are independent at this level; linking side gates existence), **Q6 evidence** (the repulsive/WRITTEN `f` is the stable branch; the focusing benchmark-verbatim branch expels charge or collapses; LoE v5 itself writes `f = gs²`), **Q9 partially self-read** (the (Ω,G) table + `k` = far-field decay extracted from corpus #10; the definitions remain the ask, now coupled to Q11), **NEW scalar-sector finding** (the naive averaged Hamiltonian is unstable in the timelike components, which is exactly where Gauss puts the divergence charge → sharpens Q7). Earlier same day (**M7.3 delivered**, [`tasks/m7_3_ouroboros_3d.md`](tasks/m7_3_ouroboros_3d.md) § FINDINGS: the verbatim-ODE pre-gate PASSED with convention pins (same-phase azimuthal doublet; coupling `−A·J`; FOCUSING `f`), the 3D lattice reproduces the M6 ledger to 4.7e-5, and three honest discoveries landed: **Q11 opened** (the charged `H/Q = 1.6890` is a WINDOWED quantity, no decaying channel exists at the canonical point), **Q12 opened** (`0d_canonical § 2.2`'s `2ωα` form is not an EL reduction; LoE 9b § 5.1's cos/sin phases decouple), **Q13 opened** (the M6 electron is a 3D constrained SADDLE ending in focusing collapse; helicity guard inert on it); **Q8 RESOLVED** (no gauge fixing needed; the real correction was objective-level: fixed-`Q_can` extremization of `E_ω`, multiplier ω). Q6 gained decisive evidence (the operative `f` is focusing with a λ-term, not the written `(g/4)s²`). Earlier (2026-07-02 night): **M7.2 quadrature delivered**: all reproduction gates pass at O(h²·⁵)/1.4e-4; printed solution reconstructed digit-for-digit; **Q10 evidence in** (Eq 122/124/127 slips, corrected `U ≈ 0.958 m_ec²`, [`tasks/m7_2_fleury_torus.md § 3`](tasks/m7_2_fleury_torus.md)); the Bessel stretch exposed the mask's hidden surface charge. Earlier: restructured to the M5 tracker pattern (ONE priority-sorted OPEN QUESTIONS table = the ask list, Q10 opened); plan refactor (Q2 direction set, Q3/Q5/Q7 sharpened, Q8 + Q9 opened); **M7.1 gate suite ALL PASS** ([`tasks/m7_1_infra.md`](tasks/m7_1_infra.md) § Findings).

---

## QUESTION DETAILS (open questions)

> The lean [`§ OPEN QUESTIONS`](#open-questions) table links here (one anchor per ID). This section carries each question's full statement, provenance, history, and evidence, plus the concrete ask where the question is outbound.

### Q7 detail

**Charge-carrying construction (ask Marc, priority 1).** What carries `∇·F ≠ 0` when the Trkalian training wheels come off, and how does it evolve? **Reframed 2026-07-02**: the divergence identity `∇·w = −(w·∇λ)/λ` says charge needs λ varying **along** field lines, exactly the regime rigidity obstructs (Clelland-Klotz 2020, even locally), so M7.4 does **not** hunt an exact `λ(x)` ansatz; it relaxes the full functional from Trkalian seeds (+ a λ-perturbation) and **measures** the deviation via `λ_eff = F·(∇×F)/\|F\|²` ([`tasks/m7_4_charged_soliton.md`](tasks/m7_4_charged_soliton.md)). Marc's endorsement (2026-06-30, "start Trkalian, take off the training wheels") survives intact, implemented as relax-and-measure rather than construct-exactly. **VALIDATED at M7.4 (2026-07-03):** all three surviving basins are approximately-Beltrami (`\|align\| = 0.96`) with the deviation carrying the structure and the RMS divergence charge persisting; the exact-ansatz hunt was indeed the wrong frame. **The asks, updated:** (a) the concrete construction steps for a **toroidal variable-h** Beltrami seed from Sato-Yamada's eikonal + equal-scale-factor method; (b) **NEW, the sharpest one**: in an A-primary doublet the net Gauss charge lives in the scalar/longitudinal sector (`∇·E_A = −m_J²J₀`), and M7.4 measured the naive averaged Hamiltonian to be **unstable in exactly that sector** (null-J quartic-flat directions with unbounded bilinear `a₀j₀` energy): what is his prescription for the longitudinal/scalar charge-carrying construction (Gauss-eliminated `a₀`? constrained `j₀`?); (c) results beyond Kaiser 2000 on variable-α existence. **M7.6 addendum (2026-07-03,** [`tasks/m7_6_observables.md § 4`](tasks/m7_6_observables.md)**): ask (b) is partially self-resolved**: the **fixed-reservoir prescription** (`j₀` = fixed external source, `a₀` unfrozen; fixing `j₀` removes the null-direction runaway) is stable and delivers the full Coulomb sector: Gauss flux = 99.1% of the source, far-field slope −2.14, and the two-charge splitting reference-matches Poisson-in-the-same-box at a constant **1.17 ± 0.02 dressing** (the vector-sector renormalization of the effective coupling). What remains for the ask is the **self-consistent** charge (dynamic `j₀`), which is the same cure Q14 needs; the Marc conversation item is now "does the S&Y/variable-h picture single out a `j₀` profile", a sharper question than before. **Fleury-notes addendum (2026-07-05, working notes, private communication; local [`../theory/electron_beltrami/`](../theory/_CITATIONS.md); math audited at receipt): the interior-profile framing is retracted on his side too.** The notes prove a no-go theorem (for any localized field with bounded potentials, Gauss gives `Q_tot(t) = 0` identically; the earlier oscillating monopole was a cutoff artifact): the oscillating interior carries only a rotating charge-separation pattern (the Zitterbewegung cloud), and **net charge is an asymptotic datum**: a curl-free Coulomb tail living exactly where `λ(r) → 0`, with closure = a turning-point matching problem at `r = w` plus a **tail-strength quantization** (what fixes `e`: open on his side, the "α computable" prize; the tail costs `U_tail = (α/8)m_ec²` under his Case-B pinning). Structurally this endorses M7.6's fixed-reservoir architecture (our fixed `j₀` ball = an imposed tail source) and replaces the S&Y variable-h ask. **The call asks (2026-07-06):** (a) confirm the tail-matching reading; (b) the status of the tail-quantization conjecture (also the Q3 slaving candidate); (c) how the matching translates to the doublet theory, where A is effectively massive in the interior (the Q14 cross-link: his `λ(r) → 0` exterior is a massless, healthy vacuum).

### Q5 detail

**Divergence-ful knots (✅ RESOLVED-delivered at M7.4, 2026-07-03; kept for history).** Does a divergence-ful field still admit clean, stable knots, or does nonzero `∇·F` destabilize the Hopfion? Sharpened by the corpus math ([`m7_background.md § 2`](m7_background.md)): **exact** variable-λ Beltrami knots are heavily obstructed (rigidity) and finite-energy exact Beltrami fields don't exist at all (Nadirashvili), so the question is properly about the **relaxed minimizer of the full functional**. **Pisello** answered YES in principle (corpus #16); Kaiser 2000 gave perturbative small-charge existence; M5.11's P2 warned that smooth knots expand without confinement. **The lattice answer (M7.4,** [`tasks/m7_4_charged_soliton.md § 2, § 4`](tasks/m7_4_charged_soliton.md)**): `∇·F ≠ 0` does NOT destabilize.** The divergence-ful Bateman/Hopf seed relaxes to a stable, dilation-stable, approximately-Beltrami soliton (`E = 1.7988`, `\|g\| = 1e-7`, align 0.960) with persistent RMS divergence charge; the confinement + helicity pair supplies exactly what M5.11's functional lacked. Honest caveat: the final state **reconnects into the same Taylor-dressed family** as the unknot seeds (`E = 0.802\|H_A\|` universal), so knot-SECTOR persistence was not demonstrated: fixing only global `H_A` permits reconnection; topology-preserving constraints (linking/cross-helicity pinning) are the designed follow-up. Second residual: the **net-Gauss-charged** knot (scalar sector) pends the Q7(b) prescription. The former ask to Marc becomes a share + a sharper discussion item (the reconnection observation bears directly on his knot program).

### Q3 detail

**The two charges (ask Marc, priority 2).** Are Fleury's divergence charge and Ouroboros's helicity/linking charge **forced equal**, or independent observables that must be reconciled? **Pisello's toroidal knot** ([`m7_background.md § 2`](m7_background.md), corpus #16) is a published precedent that **unifies** them; the Duda deck's Gauss-Bonnet route (corpus #22) converges on the same divergence-charge = topological-charge reading. **The M7.4 measurement is in** ([`tasks/m7_4_charged_soliton.md § 2-2b`](tasks/m7_4_charged_soliton.md)): across the seed matrix the two are **independent at this level**: helicity/linking gates EXISTENCE (zero-helicity seeds evaporate; helicity-carrying seeds survive), while the RMS divergence charge coexists without being slaved to it (the pure-M6 seed carried the largest seeded charge and died; the ck seed a small one and lives). Caveat: measured in the pure-vector sector (net Gauss charge frozen out); the Pisello-style slaving may live exactly in the untested scalar sector (Q7(b)). **The ask, updated:** given this measured independence, does he read Pisello/Gauss-Bonnet as predicting slaving only for the net-Gauss (scalar-sector) charge, and what discriminating measurement would he want on the relaxed states? **Fleury-notes addendum (2026-07-05):** the working notes **agree structurally with the measured independence**: in his variable-λ picture the two observables have different carriers (divergence charge = `A_r` oscillating across the **λ-gradient**, `ρ = ε₀(λ'/λ)∂_tA_r`, derived not chosen; helicity = the **winding**, `A·B = sλ₀\|A\|²`), and Theorem 1 (constant-λ/Trkalian fields are chargeless, ever) makes the separation exact in the constant-λ limit. The deeper slaving Q3 asked about now has a named candidate: the **tail-quantization conjecture** (a helicity/flux quantization condition linking the interior winding number to the exterior tail strength through the matching surface), proposed, unproven, and exactly the discriminating structure to test on the lattice once the tail matching (Q7) is prescribed. Confirm his reading at the 2026-07-06 call.

### Q10 detail

**FLDB energy convention (✅ RESOLVED 2026-07-04 by the author; kept for history, anchor stays valid).** Originally: which complex-field convention do Eqs 31/32 use? **M7.2 delivered the evidence (2026-07-02, [`tasks/m7_2_fleury_torus.md § 3`](tasks/m7_2_fleury_torus.md)) and the question sharpened**: the appendix declares the standard phasor average `E² = ½E·E*` (its Eq 113-115), but **Eq 122/124 drop the square on `(1+R/R₀)` in `E_φE_φ*` and Eq 127 drops the ½ on the B term**; the two slips produce Eq 31. With the appendix's own convention applied exactly, `U_phys = (6/5)×Eq 32` (thin torus): **`U/m_ec² = 0.795 → 0.958`**, closing most of the paper's acknowledged energy gap. The constraints (Q, μ, L) are slip-free, so the solved parameters stand. Verified three ways (closed form, independent 2D quadrature to 2e-16, 3D lattice to 1.4e-4 at O(h².5)). **The ask is now**: confirm Eq 122/124/127 and whether `U ≈ 0.958 m_ec²` is the intended prediction. Marc is the author; the cheapest, highest-value ask on the list. Opened 2026-07-02; evidence delivered same day by M7.2 (which ran non-blocked, computing both conventions as planned). **Resolution (2026-07-04, author email replying to the [Phase 1 report](tasks/m7_phase1_report.md)):** the bug is **confirmed**: it was caught internally after publication and the correction was never pushed, so the stale `0.795` stayed in the paper. The author's corrected figure is **`U ≈ 0.95 m_ec²`** vs our independently-computed **`0.958`** (the ~1% gap sits inside the thin-torus approximations); he read the agreement as a credit to the lattice integrators. M7.2's reproduction and the corrected convention are the numbers of record.

### Q1 detail

**Substrate + target manifold (ask Marc, priority 5).** The Ouroboros doublet `(A_μ, J_μ)` read via Riemann-Silberstein (candidate B) vs single-field RS `F = E + icB` (candidate A); does Clebsch/`ψ` (D) enter only as a knot **seeder**? And what **target manifold**: Pisello S² vs Faber S³ ([`m7_background.md § 2`](m7_background.md))? Narrowed to **B** by Marc's **A-primary** commitment (`A` fundamental, `F = dA`, charge derived); RS kept as a derived diagnostic. **M7.1 built the substrate as B** (the 16-component harmonic doublet, [`scripts/m7_1_harmonic_lattice.py`](scripts/m7_1_harmonic_lattice.py) via [`tasks/m7_1_infra.md`](tasks/m7_1_infra.md)); a structural argument now also favors B: Nadirashvili's theorem makes the pure-Maxwell finite-energy Beltrami electron impossible, so the doublet's confinement sector is required for existence ([`m7_background.md § 5b`](m7_background.md)). Formal closure waits on M7.3/M7.4 confirming the doublet carries both charges. **The ask:** his read on the S²-vs-S³ target-space choice. **Fleury-notes addendum (2026-07-05):** the working notes reconfirm the substrate commitments (A-primary, `φ = 0` temporal/gauge-free reading, real trig throughout, no phasors) but are silent on the target manifold; the S²-vs-S³ ask stands for the call.

### Q4 detail

**Marc's source material (ask Marc, priority 6; logistics).** The Beltrami / ABC source papers and further material from Marc. PARTLY IN: Sato-Yamada landed (arXiv:1809.03136 + the note in [`../theory/sato_yamada_beltrami.md`](../theory/sato_yamada_beltrami.md), [M7.0 corpus](tasks/m7_0_bootstrap.md) #11); more material expected (corpus #13). **The asks:** the further papers he mentioned, and the status of bringing in the Spanish Beltrami school (Enciso & Peralta-Salas, corpus #6) as collaborators. **MOSTLY DELIVERED 2026-07-05:** the promised material arrived as two working-note documents (the thin-torus ansatz recommendation + the closure results: no-go theorem, tail program, Case-B pinning, corrected 3:1), persisted local-only to [`../theory/electron_beltrami/`](../theory/_CITATIONS.md) per the corpus provenance rule (author copies, private communication, gitignored). Content routed to Q7 (tail program), Q3 (slaving candidate), Q15 (Case-B pinning), Q14 (variable-λ cure candidate) and the M7.8 task. Residual ask = the Enciso & Peralta-Salas logistics only.

### Q9 detail

**Werbos-v5 calibration dictionary (ask Werbos).** v5's canonical point is `g = 1.0625` (`H/Q = 1.6969`) vs M6's repo-validated canonical `g = 1.0` (`H/Q = 1.6890`), vs physical `1.6875`; and the `(Ω, G)` bifurcation islands need the `(Ω, G) ↔ (ω, g, m_J, λ)` map. **Self-read attempted at M7.4 (2026-07-03,** [`tasks/m7_4_charged_soliton.md § 3`](tasks/m7_4_charged_soliton.md)**):** the cited Zenodo record 20866581 now hosts a Lean-theorem v2, not the scan; the corpus doc (#10) carries the definitive island table (electron `Ω = 1.050, G ∈ [1.150, 3.250]`, stable, `k > 0`; muon `Ω = 0.914, G ∈ [0.100, 3.600]`, resonant, `k < 0`) and pins `k` = the **far-field decay constant**, but never defines `(Ω, G)`; the electron G-island does **not** bracket the canonical `g = 1.0625`, so `(Ω, G) ≠ (ω, g)` naively. **Coupling to Q11:** `k > 0` on the electron island is an implicit claim that a LOCALIZED charged branch exists somewhere, exactly what Q11(b) asks; the dictionary is required to go check it on the lattice. **The ask, now surgical:** define Ω and G in terms of `(ω, g, λ, m_J)`, and give one concrete electron-island parameter point we can run. Until resolved, the M6 calibration stays primary; the islands are the M7.13 falsifiable structure. Opened 2026-07-02; self-read 2026-07-03. **M7.5 addendum:** a candidate dictionary anchor now exists: the measured soliton-existence threshold `ω* = 0.786` (with `Q_J` diverging toward it) is exactly the kind of island boundary his scan would produce; if his stability islands are the `ω > ω*(g, λ)` region, the `(Ω, G)` map may be checkable against our threshold curve ([`tasks/m7_5_clock_stability.md § 3`](tasks/m7_5_clock_stability.md)). **Q11-micro-slice corollary (2026-07-05):** the corpus table's `k > 0` (decaying far field) on the electron island is impossible at the canonical `(λ, m_c)` for any `(ω, g)` (the decaying-channel condition is `ω < ω*` there, incompatible with existence), so a consistent dictionary MUST move `λ` and/or `m_J`, another constraint the `(Ω, G)` definitions have to satisfy.

### Q6 detail

**Potential form (✅ RESOLVED-empirical at M7.4/M7.5, 2026-07-03; kept for history; residual routed to Q12).** The `f(J·J)` potential form for M7: keep M6's canonical `f(s) = (g/4) s²` ([`0d_canonical.md § 1`](../../m6_ouroboros/research/0d_canonical.md)), or a form better suited to the toroidal sector? **M7.3 evidence (2026-07-03,** [`tasks/m7_3_ouroboros_3d.md § 1`](tasks/m7_3_ouroboros_3d.md)**):** the potential that reproduces the benchmark ODE verbatim is FOCUSING (`c1 = −λ/2, c2 = −2g` RWA), sign-flipped vs the written form, and drives the 3D collapse (Q13). **M7.4 evidence (same day,** [`tasks/m7_4_charged_soliton.md § 2-2b`](tasks/m7_4_charged_soliton.md)**): the repulsive/WRITTEN branch is the physical one**: it holds three stable soliton basins and keeps the RMS charge, while the focusing branch expels charge or collapses; and LoE v5 itself writes **`f(s) = g s²`** with λ separate (the `(g/4)` in our M6 doc is a transcription slip to fix). **The ask, now sharp:** the benchmark ODE's effective signs (which produced `H/Q = 1.6890`) are inconsistent with his own written Lagrangian, and the written Lagrangian is the one that yields stable 3D solitons: does he confirm the benchmark sign slip, and `f = gs²` + separate λ as canonical? **M7.5 addendum:** the f-choice does NOT decide the real-time vacuum question: both conventions are unconditionally tachyonic at long wavelength (bands 0.618 vs 1.618, Q14); the repulsive branch remains preferred (smaller band + all the M7.4 evidence), but the vacuum cure must come from elsewhere. **Resolution (2026-07-03, M7.5 closeout):** with the physics answered in-model (the written branch is the stable theory) and the vacuum question decoupled, nothing the program does next waits on this: RESOLVED-empirical; the intent-confirmation + `(g/4)` doc fix ride with Q12 in the Werbos package as a share/confirm item, not a blocking ask.

### Q11 detail

**Windowed charged calibration (SELF-DETERMINE since 2026-07-05, was ask-Werbos; NEW at M7.3).** The M6 charged benchmark profile at the canonical point (g=1, ω=1, λ=1, A0=B0=0.1) does not decay: `Q` grows ~linearly with the integration window and `H/Q` oscillates through the ledger value, equaling 1.6890 exactly at the benchmark's hard-coded `r_max = 12` ([`tasks/m7_3_ouroboros_3d.md § 2`](tasks/m7_3_ouroboros_3d.md), plot [`plots/m7_3_embed_convergence.png`](plots/m7_3_embed_convergence.png)). Analytic root: the far-field dispersion `(ω² − k²)(ω² + λ − k²) = 1` has both roots `k² = (3±√5)/2 > 0` at the canonical point: **no exponentially decaying channel exists**; the A-sector is a radiation field at ω. Same structure as the neutral-sector windowed-integration artifact the M6 record already caught (sandbox_v8 Q42), never flagged for the charged sector. **The asks:** (a) is `H/Q = 1.6890/1.6969` understood to be window-defined, and what fixes the window physically? (b) does a genuinely localized charged branch exist elsewhere in `(ω, λ, g)` (the localization condition needs both far-field masses positive, i.e. `ω² < 0`-type regions the real-ω scan can't reach; a Yukawa-decaying variant would need a different coupling structure)? Note the constructive side: this is Nadirashvili-consistent and strengthens the HydroBoros confinement thesis ([`m7_background.md § 5b`](m7_background.md)). **M7.5 addendum:** the repulsive-branch dispersion places the propagating channel at ω = 1 at `k² = (1+√5)/2` (the upper golden-ratio root): the delocalization is now understood as the ω = 1 line crossing the propagating band of the SAME dispersion whose negative band is the Q14 tachyon; one dispersion relation carries both findings ([`tasks/m7_5_clock_stability.md § 2`](tasks/m7_5_clock_stability.md)). **M7.6 addendum:** the channel has a measured physical consequence: two neutral solitons interact via an **oscillatory RKKY/Friedel-style exchange** (`E_int` alternating in d with period `π/k = 2.47`, aligned/anti mirrored, [`tasks/m7_6_observables.md § 4`](tasks/m7_6_observables.md)): the windowed tail is not just a calibration artifact, it mediates forces. **Re-scope (2026-07-05):** SELF-DETERMINE: ask (b) is answered by a parameter scan, not a question (a localized-branch hunt over `(ω, λ, g)`, riding the M7.13 islands task); ask (a)'s window-intent remains a share/confirm item alongside Q12. **Micro-slice run (2026-07-05 night,** [`scripts/m7_13_q11_window.py`](scripts/m7_13_q11_window.py) · [data](data/m7_13_q11_window.json) · [plot](plots/m7_13_q11_window.png)**):** an externally reported sweep (received 2026-07-05, unverified: no script, no definitions, no convention pins shared) claimed a localized charged branch at `ω = 0.8` for `g ∈ {0.25, 0.5}` with window-convergent `Q → 1.689`. Tested on the benchmark ODE + quadratures over windows `W = 8-128` at `(ω ∈ {0.8, 1.0}) × (g ∈ {0, 0.25, 0.5, 1.0})`: **REFUTED on our definitions**: every combination is window-DIVERGENT with `Q(2W)/Q(W) ≈ 2.0` (the linear-in-W radiation signature); `g` rescales the amplitude but never the window scaling, exactly as the far-field dispersion demands (no `g` in the linear sector; at `ω = 0.8` the roots are `κ² = 2.258, 0.022`, both propagating). None of the externally quoted numbers reproduce under any `(ω, g)` tried (and the quoted "converged `Q = 1.689`" is numerically the `H/Q` RATIO, not a charge). **Bonus sharpening:** the decaying-channel condition `ω²(ω² + λ) < m_c⁴` at the canonical `λ = m_c = 1` solves to `ω² < (√5 − 1)/2`, i.e. **`ω < 0.786 = ω*`: localization and harmonic-frame existence are mutually exclusive in the vector truncation at the canonical point** (the same golden-ratio dispersion carries the tachyon band, the delocalization, and now the localization boundary). Consequence for the M7.13 scan: the localized charged branch, if it exists, requires moving `(λ, m_c)`, not `(ω, g)`.

### Q12 detail

**M6 ODE / ansatz provenance (ask Werbos; NEW at M7.3).** The M7.3 sympy scan ([`tasks/m7_3_ouroboros_3d.md § 1`](tasks/m7_3_ouroboros_3d.md), [`data/m7_3_pregate_sympy.json`](data/m7_3_pregate_sympy.json)) reduced the M6 Lagrangian over six harmonic ansaetze × two quartic conventions. Results: (a) the benchmark ODE (the validated `H/Q` producer) is the verbatim EL reduction of the **same-phase azimuthal doublet** `A_φ = α(ρ)cos ωt, J_φ = β(ρ)cos ωt` with pins `κ = −1`, focusing `f`; (b) [`0d_canonical.md § 2.2`](../../m6_ouroboros/research/0d_canonical.md)'s `2ωα` chiral form matches NO candidate, with a structural obstruction (time-averaged bilinears give only ω⁰/ω² couplings; the needed `∇X₀·∂ₜX⃗` cross term is geometrically zero), so § 2.2 as written is not an EL reduction of the stated Lagrangian; (c) LoE 9b § 5.1's phase text (`A ∝ cos`, `J ∝ sin`) decouples the α-equation (`⟨cos·sin⟩ = 0`) and its μ=0 J-equation forces α ≡ 0. **The asks:** which ansatz + ODE is the intended canonical (and where the `2ωα` form comes from, e.g. a different derivation route or the "asymmetric helicity prescription"); our `0d_canonical.md § 2.2` needs a correction note either way. Also feeds Q9 (the v5 dictionary presumably shares the provenance).

### Q13 detail

**3D stability of the chaoiton (SELF-DETERMINE since 2026-07-05, was ask-Werbos; NEW at M7.3).** With the verbatim-pinned functional, the embedded M6 electron is a genuine 3D discrete critical point of `(E_ω, Q_can)` with multiplier ω (residual → 0 as h²), but **not a constrained minimum**: fixed-`Q_can` descent departs immediately in both the axisymmetric sector (converges first to a different near-critical state, then finds an axis-concentration channel) and the free 3D sector (supercritical focusing collapse, amplitude → 124× seed before the non-finite guard) ([`tasks/m7_3_ouroboros_3d.md § 4`](tasks/m7_3_ouroboros_3d.md)). Mechanism: the focusing pins (Q6) make `E_ω` unbounded below along concentration at fixed `Q_can` (L²-critical in 2D-symmetric, supercritical in 3D). The M6 ansatz carries zero A-helicity, so helicity-based anti-collapse guards are inert on it. **The asks:** (a) is the LoE 9b § 5 "constrained minimum + conjugate-point" stability claim established only within the 1D radial ansatz manifold? (b) does Werbos have a candidate stabilizing mechanism in the full 3D field space (M7's answer is the M7.4 helicity-carrying blend)? Honest caveats attached: FIRE descent shows a descent direction exists, not a growth rate; the collapse endpoint is lattice-arrested/guarded, not resolved. **M7.5 addendum (2026-07-03,** [`tasks/m7_5_clock_stability.md § 2-3`](tasks/m7_5_clock_stability.md)**):** the stability question now has a sharper frame: even the M7.4 stabilized states live above a hard existence threshold `ω* = k* = 0.786` (measured: runaway at ω = 0.70/0.75, clean solitons from 0.79 up, `Q_J` diverging toward the threshold): the clock frequency is itself the stabilizer, and BELOW ω\* nothing is stable regardless of helicity. The 1D-vs-3D part of the ask stands; the real-time part is subsumed by Q14. **Re-scope (2026-07-05):** SELF-DETERMINE: the physics is measured in-model (the saddle, the helicity fix, the ω\* threshold); what remains is a provenance question (was the conjugate-point claim 1D-only?), which rides with Q12 as a share/confirm item.

### Q15 detail

**The units contract (ask Marc, priority 1 since 2026-07-05; NEW at M7.7).** M7.6 measured the rotating electron's dimensionless spin structure: the per-quantum angular momentum is exactly one (`⟨j_z⟩ = 0.9939/0.9934`, A/J sectors), and the Poynting total is `ωL_z/E_ω = 2.07` (the energy budget closes exactly; the coupling energy is negative, so `E < U_quad`). Turning these into physical statements needs ONE mapping choice, tabulated in [`m7_theory_canonical.md § 4`](m7_theory_canonical.md):

| Choice | Consequence |
| --- | --- |
| `ω = ω_Compton = m_ec²/ℏ` | total field `L_z ≈ 2.07 ℏ`; the per-quantum `j_z = 1` reads as ℏ |
| `ω = ω_Dirac = 2m_ec²/ℏ` (Zitter) | total `L_z ≈ 1.03 ℏ`; the per-quantum `j_z = 1` reads per 2ω-cycle |

Our on-record recommendation is the Dirac/Zitter mapping (field bilinears oscillate at 2ω, matching the Zitterbewegung structure and the M5.8 clock; FLDB's own targets are stated as `ω/ω_D` ratios), which lands the total at ℏ within 3%. **The ask:** which mapping does the FLDB picture intend as canonical? The answer decides the ℏ/2-vs-ℏ reading of the spin cell and fixes the absolute mass anchor (`E_ω = 6.3246` program units → `m_ec²`), so it gates the M7.15 mass/spin/μ cells. Related: Q10 (the same paper's energy convention). Opened 2026-07-04 (M7.7 closeout). **Fleury-notes addendum (2026-07-05):** the working notes **pin Case B, `ω = ω_Compton`**, via three closure postulates (P1 core-null → `ω = cλ₀`; P2 one quantum `U = ℏω = m_ec²`; P3 spin `S_z = ℏ/2`), selecting `λ₀ = 1/λ̄_C, σ = 2λ̄_C, w = 4λ̄_C` uniquely, and **exclude Case A (`ω = 2ω_C`, the Zitter pinning we recommended)** on the tail budget: Case A needs `U₊ − U₋ = m_ec²` with nothing left for the `(α/8)m_ec²` tail (it survives only on a flagged, unpursued half-quantum branch). Two readings reconcile at the observable level: the `2ω_C` Zitterbewegung is recovered in the **bilinears** (`cos(2φ − 2ωt)`), which is where our "bilinears oscillate at 2ω" observation lives. Key structural difference from our measured state: his spin ℏ/2 is the **helicity-pair asymmetry** `(U₊ − U₋)/ω` of a two-mode state with `U₊/U₋ = 3 + α/2 + 4f_bb` (the 3:1 ratio is forced two-line algebra from P2 + P3, not a free claim; we re-derived it at receipt), while our M7.6 electron is a single rotating `j_z = 1` mode. **M7.8** (top of the Phase 2 backlog) builds the repaired helicity pair and measures `U₊/U₋` in-model; run/no-run + scope to be set at the 2026-07-06 call. Pre-call notes, unvetted by the author: treat as evidence, not resolution, until he confirms live.

### Q14 detail

**The vacuum tachyon (SELF-DETERMINE since 2026-07-05, was ask-Werbos; NEW at M7.5).** M7.5 evolved the M7.4 winner with the constrained Minkowski leapfrog of the written theory (translation validated exactly: `⟨E_real⟩ = E_ω` to 1.85e-14; integrator drift O(dt²)) and found the state destroyed in ~2 periods, with the cause fully analytic ([`tasks/m7_5_clock_stability.md § 2`](tasks/m7_5_clock_stability.md), plots [`plots/m7_5_tachyon_scan.png`](plots/m7_5_tachyon_scan.png)): the linearized transverse vacuum has the frequency matrix `M(k) = [[k², −m_c²], [−m_c², k² + 2c1]]` and `det M(0) = −m_c⁴ < 0` for **any** quartic coefficients: a bilinear `A·J` coupling with a massless A is **unconditionally tachyonic at long wavelength**. Canonical numbers: band `k² < (√5−1)/2` (repulsive; edge `k* = 0.7862`) or `k² < (1+√5)/2` (focusing; `k* = 1.2720`); max amplitude growth rate = `k*` at `k = 0`. Lattice confirmation: vacuum-noise growth rate **0.7850 measured vs 0.7862 analytic (0.15%)**, amplitude-independent from ε = 1e-6 to 1e-2: the instability is **linear**, so the v5 Test 1 `β*` threshold does **not exist** in the pure-vector truncation. The harmonic frame evades it because the oscillation supplies diagonal stiffness: the `E_ω` quadratic form is PSD iff `ω ≥ ω* = k*`, verified by the fixed-`H_A` scan (runaway at ω = 0.70/0.75, solitons from 0.79; predicted 0.7862 bracketed). **The asks:** (a) how does the full model cure the vacuum: an A-mass (condensate/Higgs-like vacuum), the scalar/Gauss constraint sector, a finite-universe IR cutoff (`k_min > k*` needs a universe of size `L ≲ 8` natural units, clearly not it), or parameter islands where `m_c⁴ <` (A-mass)·(J-mass)? (b) is this instability known in his 319-family scan (his `k > 0` stable-island label suggests a cured region: Q9's dictionary would let us check)? (c) note the constructive reading: the ω\* threshold makes the de Broglie clock LOAD-BEARING (particles exist because they tick fast enough), which is a strong HydroBoros-thesis statement if the full theory confirms it. **Cure-candidate addendum (2026-07-05, via the Fleury working notes):** his variable-λ construction is, in Proca language, a **spatially-varying A-mass `m_A(r) = ℏλ(r)/c` that vanishes outside the soliton** (`λ(r)² = λ₀²(1 − r²/w²)`, `λ → 0` at the edge), leaving a massless, pure-Maxwell (tachyon-free) exterior vacuum: a concrete profile proposal for the "A-mass from a condensate" cure family, testable on the lattice as a variable-coupling extension. Blocks any real-time program (M7.12 annihilation) until answered or cured in-model. **Re-scope (2026-07-05, after the first Werbos round-trip returned no resolutions):** the cure question is computable, so it moves to SELF-DETERMINE: Phase 2 linearizes the vacuum WITH the scalar/Gauss sector on our own lattice (does it lift `det M(0) = −1`?) and tests the variable-λ mass profile, as the M7.12 pre-step. The asks (a)-(c) convert to share/confirm items: results go out with the next delivery, not as questions.

### Q8 detail

**Gauge / constraint handling (✅ RESOLVED at M7.3, 2026-07-03; kept for history).** The question: which scheme for the coupled minimizer, Coulomb gauge on `a⃗` + kept `a₀`, projection, or penalty? Evidence trail: M7.1 gate G5 measured `E_ω` exactly gauge-invariant at `m_J = 0` (machine zero) and broken by `m_J²A·J` off-shell (`ΔE/E ≈ 1.3e-3` on random fields); the static curl sector self-fixes (transverse AD gradient; the Woltjer relaxation converged gauge-drift-free), [`tasks/m7_1_infra.md`](tasks/m7_1_infra.md) § Findings. **Resolution (M7.3,** [`tasks/m7_3_ouroboros_3d.md § 1, § 4`](tasks/m7_3_ouroboros_3d.md)**): no explicit gauge fixing.** The coupled fixed-`Q_can` relaxation ran gauge-unfixed with no stall or drift pathology; the correction the pre-gate actually forced was **objective-level**, not gauge-level: extremize `E_ω` at fixed `Q_can` (multiplier ω), never unconstrained `E_ω` (its EL carries the ω²-sign flip). Residual: if a future M7.4+ run shows a gauge stall, reopen with the Coulomb-projection candidate. Opened 2026-07-02 (plan refactor); resolved 2026-07-03.

### Q2 detail

**4th-order stabilizer (✅ RESOLVED at M7.4, 2026-07-03; kept for history).** Is a 4th-order term needed at all, and in which form? **Direction set** (2026-07-02 theory review): the drafted `\|F×(∇×F)\|²/\|F\|²` term **vanishes identically on every Beltrami configuration** and is **singular** where `\|F\| → 0`, so it provides zero Derrick pressure on exactly the target family; demoted to an optional experiment. Working stabilization = **helicity anti-collapse** (Arnold `E ≥ λ₁\|H\|`) + **Ouroboros confinement anti-expansion**, with **Nadirashvili's theorem** forcing the confinement term to exist ([`m7_background.md § 5b`](m7_background.md)). **Resolution (M7.4,** [`tasks/m7_4_charged_soliton.md § 2`](tasks/m7_4_charged_soliton.md)**): NOT needed.** The helicity + confinement pair stabilizes with no 4th-order term: every surviving state's constrained Derrick curve has an interior minimum (dilation probe, measured), and the guard is demonstrably load-bearing (both zero-helicity seeds evaporate; all three helicity-carrying seeds hold). The optional experiment was never required. Evidence trail: M7.1 Woltjer gate (2026-07-02) → M7.4 dilation probes (2026-07-03).

---

Cross-refs: roadmap [`m7_roadmap.md`](m7_roadmap.md) · background [`m7_background.md`](m7_background.md) (§ 2 math boundaries, § 5 dynamics) · task docs [`tasks/m7_1_infra.md`](tasks/m7_1_infra.md) (Q1/Q2 evidence, Q8 G5 evidence) · [`tasks/m7_2_fleury_torus.md`](tasks/m7_2_fleury_torus.md) (Q10) · [`tasks/m7_3_ouroboros_3d.md`](tasks/m7_3_ouroboros_3d.md) (Q8 resolution; Q6 evidence; Q11/Q12/Q13 openers; Q9 context) · [`tasks/m7_4_charged_soliton.md`](tasks/m7_4_charged_soliton.md) (Q3/Q5/Q7) · [`tasks/m7_5_clock_stability.md`](tasks/m7_5_clock_stability.md) (Q14 opener; Q11/Q13 evidence; Q9/Q6 addenda) · [`tasks/m7_6_observables.md`](tasks/m7_6_observables.md) (Q7(b) partial self-resolution; Q11 RKKY addendum) · the Fleury working notes 2026-07-05 (Q10 resolution context; Q7/Q3/Q15/Q14 addenda; local-only, [manifest](../theory/_CITATIONS.md)) · the M5 / M6 trackers this mirrors: [`m5_question_tracker.md`](../../m5_liquid_crystal/research/m5_question_tracker.md) (M5) · [`0b_question_tracker.md`](../../m6_ouroboros/research/0b_question_tracker.md) (M6).
