# M9.103--M9.105 task contract

## Objective

Execute the next three roadmap targets without predetermined physical promotion.

## Required outputs

1. An unrestricted two-component charged-state solver that uses no winding or spin projection after initialization and reports stationary plus orbital gates.
2. A grid/time refined local packet Thomas--BMT comparison against the exact Dirac generator, with the covariant Thomas law explicitly typed as an external postulate.
3. A dependency-audited external-calibration protocol that rejects circular and target-fitted anchors and withholds predictions until all required physical scales are independent.

## Acceptance

- every campaign emits machine-readable JSON;
- campaign execution and physical sub-gates are separate;
- current Physlib and ZIL heads and source blobs are pinned;
- ZIL graph queries expose sub-gates, postulates, anchors and predictions;
- no failed physical gate is converted into a software failure or a positive claim;
- no internal identity is called external calibration.
