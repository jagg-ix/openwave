# M9.45 finding

The neutral Gaussian fixed-charge survey is diffuse at `Q=20` and first becomes
bound at surveyed charge `Q=60`. At `Q=100`, the candidate has energy per charge
below `0.95`, an interior amplitude/radius minimum, and a positive log-variable
Hessian. Removing attraction eliminates binding; removing sextic stabilization
produces runaway energy. The candidate survives ±5% parameter variations.

This is a variational neutral candidate, not a physical dark-matter result.
Focused validation: `8 passed`.
