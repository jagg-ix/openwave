# M9.60 finding

PhysLib proves uniqueness of `λ|ψ|²ψ` inside the declared local gauge-covariant cubic class. For a local polynomial action, a positive cubic density term is the lowest saturation that bounds the focusing quadratic density action below, producing the cubic--quintic wave generator used by M9.59. This selects the form class, not the numerical coefficients: several positive `(alpha,beta)` pairs satisfy the same structural requirements.
