# M9.40 finding

The Cornell control recovers potential slope `0.90116781` and force plateau
`0.90046433` for configured string tension `0.9`. The string-breaking radius is
`3.10467588`; above threshold, pair-energy plus residual flux closes exactly.
With zero string tension, the force returns the Coulomb `r^-2` asymptote.

This is a confinement control, not QCD. Focused validation: `8 passed`.
