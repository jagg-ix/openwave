# M9.29 finding: field observables close, exchange statistics remain open

For the normalized spin-up field, `spin_z=0.5`, the Pauli-current magnetic moment is `0.499999968349`, orbital angular momentum is zero, and total `J_z=0.5`.

The 2π state-to-minus-state error is `1.225e-16`; the 4π return error is `2.449e-16`. Bilinears return after 2π.

This validates a Pauli spinor observable control. It does not derive exchange statistics, a stable particle, or an emergent electron g factor.

Focused validation: `8 passed`. No CI workflow was run or inspected.
