# M9.75 finding

The earlier global `H¹ → H¹` generator target is rejected: Fourier modes make the Laplacian ratio grow as `k²`, while the `H¹ → H⁻¹` ratio is `k²/(1+k²) < 1`. The unit-mass sphere is not weakly closed, and translated negative-energy states disprove unconditional weak lower semicontinuity. Localized interaction convergence instead closes strong no-loss and normalization.