# M9.39 finding: current lepton hierarchy laws fail prediction

The normalized benchmark is `1 : 206.768282708 : 3477.228275324`.
A two-parameter geometric law misses by a maximum factor `2.308`; leave-one-out
errors reach factor `12.295`. A three-parameter log-quadratic interpolates all
three values exactly but has zero residual degrees of freedom and turns over at
generation `2.6248`, predicting a nonmonotone extrapolation.

Mapping the M9.27 scale law requires one effective knob per generation; a shared
geometric knob law misses by factor `3.003`. No predictive hierarchy is selected.
Focused validation: `8 passed`.
